// Brand strings shared by every surface (landing, /keep, /oauth, policy pages).
// Landing-page copy lives in landing-copy.ts.
export const siteCopy = {
  brand: {
    // The product is "Living Memory". "Living Memory Engine" is the open
    // source engine underneath it, and only appears where the engine itself
    // is what is being named.
    wordmark: "LIVING MEMORY",
    name: "Living Memory",
    engine: "Living Memory Engine",
  },
} as const;
